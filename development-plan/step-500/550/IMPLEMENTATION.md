# Step 550 - Implementation Plan

1. Create/update `Home`, `Rules`, and `Nav` component files and CSS.
2. Add recent-games API consumption with loading/error states.
3. Wire active-game indicator from current-user game metadata.
4. Register `/` and `/rules` routes in `App.jsx`.
5. Add tests for routing and nav visibility states.
