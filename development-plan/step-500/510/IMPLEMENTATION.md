# Step 510 - Implementation Plan

1. Create `frontend/src/components/ChessBoard.jsx` with prop-driven rendering.
2. Implement deterministic FEN parser helper.
3. Add orientation transform + coordinate labels.
4. Add `disabled` click short-circuit and highlight/phantom classes.
5. Add `frontend/src/components/ChessBoard.css` and tests.

## Definition of Done

- Acceptance criteria in `README.md` met.
- `TESTING.md` commands pass or have explicit blocked reason.
- Evidence logged in `step-500/PROGRESS.md`.
