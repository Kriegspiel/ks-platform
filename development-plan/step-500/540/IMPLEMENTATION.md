# Step 540 - Implementation Plan

1. Create `PromotionModal.jsx/.css` with click-outside/escape cancel behavior.
2. Add pre-submit promotion detection in `Game.jsx`.
3. Gate move submit until promotion selection finalizes.
4. Add last-move + illegal feedback rendering.
5. Add targeted tests for promotion and feedback paths.
