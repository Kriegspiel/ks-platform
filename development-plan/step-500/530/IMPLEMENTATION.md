# Step 530 - Implementation Plan

1. Implement `usePhantoms` with deterministic transitions.
2. Build `PhantomTray` selectable piece UI.
3. Integrate right-click removal and square placement in `Game.jsx`.
4. Add persistence read/write guards for malformed localStorage.
5. Add hook/component tests for transition matrix + persistence.
